# Ясак Кликер

Инкрементальная игра в сеттинге сибирской пушной торговли.

## Система управления звуком

Игра включает полнофункциональную систему управления звуком с возможностью:

- Включения/выключения фоновой музыки
- Включения/выключения звуковых эффектов
- Сохранения настроек между сессиями

### Кнопки управления звуком

В правом верхнем углу экрана находятся две кнопки:

- **🎵 Музыка** - управление фоновой музыкой
- **🔊 Звуки** - управление звуковыми эффектами (клики, достижения)

Кнопки меняют внешний вид в зависимости от состояния:
- Активное состояние: яркая кнопка с эмодзи
- Отключенное состояние: полупрозрачная кнопка с другим эмодзи

### Добавление фоновой музыки

#### Способ 1: Через JSON плейлист (рекомендуется)

1. Добавьте музыкальные файлы в папку `assets/sounds/music/`
2. Отредактируйте файл `playlist.json`:

```json
{
  "musicTracks": [
    {
      "id": "track1",
      "name": "Название трека 1",
      "src": "assets/sounds/music/track1.mp3",
      "duration": 180
    },
    {
      "id": "track2",
      "name": "Название трека 2",
      "src": "assets/sounds/music/track2.mp3",
      "duration": 200
    }
  ],
  "settings": {
    "shuffle": true,
    "repeat": false,
    "volume": 0.3,
    "crossfade": false
  }
}
```

#### Способ 2: Через HTML (fallback)

Если JSON файл не найден, система проверит аудио элементы в HTML:

```html
<audio id="music-track1" src="assets/sounds/music/track1.mp3" preload="none" aria-hidden="true"></audio>
<audio id="music-track2" src="assets/sounds/music/track2.mp3" preload="none" aria-hidden="true"></audio>
```

### Технические детали

- Музыка воспроизводится в случайном порядке
- После завершения трека автоматически начинается следующий
- Громкость музыки установлена на 30% от максимума
- Громкость звуковых эффектов установлена на 70% от максимума
- Настройки сохраняются в localStorage

### Структура файлов

```
assets/
├── sounds/
│   ├── music/          # Папка для музыкальных файлов
│   │   ├── track1.mp3
│   │   ├── track2.wav
│   │   └── track3.ogg
│   ├── bark.mp3        # Звук критического клика
│   └── bell.wav        # Звук получения достижения
└── ...
```

## Игровые механики

- **Клики**: Основной способ получения пушнины
- **Ловушки**: Пассивный доход (0.1 шкурки/сек за ловушку)
- **Охотники**: Увеличивают силу клика
- **Ясачные зимовья**: Умножают пассивный доход
- **Охотничьи псы**: Добавляют шанс критического клика (x5)

## Достижения

Система достижений отслеживает прогресс игрока и открывает награды при выполнении условий.

## Сохранение

Игра автоматически сохраняет прогресс каждые 30 секунд и при изменении состояния.
